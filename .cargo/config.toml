[target.x86_64-unknown-none]
# 裸机 target 用 rustc 自带 rust-lld 链接，无需宿主链接器。
rustflags = [
    "-C", "link-arg=-Tkernel/linker.ld",
    # 内核固定地址链接：禁用 PIE（避免 R_X86_64_32 重定位错误）
    "-C", "relocation-model=static",
    "-C", "relro-level=off",
]
