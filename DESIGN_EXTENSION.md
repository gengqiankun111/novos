# 山水观心操作系统长期设计扩展（DESIGN_EXTENSION）

> 本文档是 DESIGN.md 的**远期延伸**：在 v1.0（minimal/full 稳定）之后，沿**四条主线**长期演进。
> 对应开发路线见 [DEVELOP_EXTENSION.md](DEVELOP_EXTENSION.md)，数据结构级远期项见 [EXTENSIONS.md](EXTENSIONS.md)。
>
> 前三条主线源于 DESIGN §21"用户预期管理"对**致命性 Bug / 核心困惑 / 高频需求**的精准预判，
> 演进哲学分别是：**从防御到免疫、从解释到透明、从可用到好用**。
>
> **主线四（图形与桌面体验）**由产品线扩展（DESIGN §2.4 山水观心 Desktop）驱动，演进哲学为
> **从"网关专用"到"可视交互"**；图形版内存独立核算（≥128MB），不适用 32MB 限制。

---

## 主线一：致命性 Bug —— 从"防御"到"免疫"

用户把 Linux 使用惯性带到 山水观心操作系统上（glibc/musl 不兼容、Ext4 挂载失败、Redis OOM）。
长期目标是建立**契约式交付体系**：不兼容的软件在到达用户之前就被拦截或自动适配。

**触发条件（任一满足即启动）**：
- `novos-check` 拦截日志中非 musl 二进制占比 > 5%；
- 用户反馈"安装软件困难"频次超过 3 次/月；
- 社区请求添加第三方软件源。

### 1.1 应用商店式的签名与兼容性层

**现状**：`novos-check` 启动前扫描 ELF 的 `PT_INTERP`，拦截非 musl 二进制（DESIGN §21.1）。

**长期演进**：把 `novos-check` 从"拦截器"升级为"适配器"，并逐步建立**官方软件仓库**（DESIGN §22）
——这是从"出色内核"到"成功平台"的必经之路。

**为什么需要官方仓库（对比官网链接清单）**：预编译预配置（告别依赖地狱）、官方测试签名
（主动保障，替代被动拦截）、内置最佳实践（一键部署）。

**三阶段演进**：

- **阶段一（MVP，1.0 初期）**："官方推荐软件清单"页（软件/功能/官网地址/官方验证 musl 静态
  二进制下载链接）+ `novos-build`（源码 → 兼容软件包）；
- **阶段二（生态构建期）**：社区软件仓库（opkg/容器化），`novos repo-add`/`novos install`；
  核心包维护：官网稳定源码 → `musl-gcc` 静态编译单可执行 → 打包 + 私钥签名 → 上传；
- **阶段三（终极形态）**：云端原生应用商店，`novos deploy redis` 自动拉取最新最安全镜像部署。

- **轻量兼容层**：评估为 glibc 程序提供一个极简的 glibc→musl syscall 翻译层，或**容器化沙盒**
  ——让不兼容程序在受限环境运行，崩溃不波及宿主机。
- **签名与认证**：仓库内软件经官方预审核 + 签名，用户只能经 `novos pull`/`novos install`
  拉取已验证镜像，从源头杜绝"乱装软件导致崩溃"。

**立即可行**：选定 Redis 7.2.4+、给出官方下载链接、编写《为 山水观心操作系统构建 Redis》musl 静态编译指南。

### 1.2 存储系统的自适应与硬化

**现状**：Ext4 驱动强制 `data=journal`（DESIGN §21.3），遇 `data=ordered` 盘拒绝挂载。

**长期演进**：

- **自适应挂载**：检测到 `data=ordered` 时，后台自动启动转换进程升级为 `data=journal`，
  而非直接拒绝；提升用户体验。
- **引入 F2FS**：工业场景常用 SD 卡/eMMC，F2FS（Flash Friendly File System）原生支持
  断电恢复 + 磨损均衡，比 Ext4 更合适——作为 full 模式的长期文件系统选项。

### 1.3 容器资源限额的"固若金汤"

**现状**：预置只读 `redis.conf` 强制内存上限（DESIGN §21.2）。

**长期演进**：资源限额下沉到 **Cgroup v2 内核层**，与用户态配置解耦。
- 内核识别 `redis-server` 进程，自动应用安全策略——无论用户如何传参，内存上限无法突破
  系统分配的 Cgroup 限额；彻底杜绝用户误操作导致宿主机 OOM。

---

## 主线二：核心困惑 —— 从"解释"到"透明"

用户对"不能本地编译 / 只有单核 / 没有 Docker 生态"的困惑，源于拿 山水观心操作系统与完整 Linux 发行版对比。
长期方向是重塑认知：把"受限"变成"轻量、高效"的优势。

**触发条件（任一满足即启动）**：
- 用户反馈"交叉编译环境搭建困难"频次超过 5 次/月；
- `/etc/motd` 提示后仍有用户在设备上执行 `go build`；
- SMP v2.0 发布后，用户不知道如何利用多核。

### 2.1 云端原生开发体验（Cloud-Native DX）

**现状**：`/etc/motd` 提示去宿主机交叉编译（DESIGN §21.4）。

**长期演进**：把"交叉编译"痛点转化为独特优势。

- **官方云构建服务**：浏览器写代码 → 云端 IDE/CI/CD → 自动交叉编译 + OCI 打包 + OTA 直达设备；
  彻底消除本地搭建交叉编译环境的需求。
- **"代码即镜像"工具链**：`novos-build` 一条命令完成源码 → OCI 镜像 + 自动 `novos-check` 验证。

### 2.2 多核能力的"软着陆"

**现状**：v1.0 单核，`/proc/cpuinfo` 报告全核但 online=1（DESIGN §21.5）。

**长期演进**：

- **透明调度**：v2.0 SMP 默认开启跨核负载均衡，单核容器应用无需改配置即享多核性能。
- **CPU 亲和性（CPU Affinity）**：通过 Cgroup 将特定容器绑定到特定核心，满足工业控制等
  延迟敏感场景。

### 2.3 构建"OCI 原生"生态（非"Docker 兼容"）

**现状**：不做 Docker CLI 兼容（DESIGN §21.6）。

**长期演进**：与其费力兼容 Docker，不如引领"OCI 原生"嵌入式生态。

- **与 Balena 等生态融合**：BalenaEngine 已证明精简 OCI 引擎比完整 Docker 更有价值；
  山水观心操作系统运行时与 BalenaCloud 等平台集成，提供设备管理/OTA 等企业级能力。
- **标准化工具链**：推动 `novos` 命令行成为嵌入式 Rust/Go 项目的事实标准构建工具（如 cargo 之于 Rust）。

---

## 主线三：高频需求 —— 从"可用"到"好用"

用户对"硬实时 / 网络诊断 / 系统升级"的需求，是从原型走向产品必须跨越的门槛。

**触发条件（任一满足即启动）**：
- 工业用户明确提出 Modbus 响应延迟 > 100ms；
- 运维团队反馈排查问题需重启设备查看日志；
- 用户要求系统升级支持断电安全。

### 3.1 实时性的深度定制

**现状**：预留 RT 类，第一版只有 CFS（DESIGN §21.7）。

**长期演进**：

- **SCHED_FIFO/RR 完整实现**：POSIX 实时调度类，关键任务（Modbus 采集）以最高优先级运行。
- **中断线程化（Threaded Interrupts）**：大部分中断处理转为内核线程并赋实时优先级，
  减少中断关闭时间，提升整体响应能力。

### 3.2 可观测性的全面增强

**现状**：`packet_trace` 丢包日志调试开关（DESIGN §21.8）。

**长期演进**：

- **eBPF 子集**：完整 eBPF 过重，但可移植极简 eBPF 解释器，动态注入探针、收集内核函数追踪/
  性能计数——比"打印日志"更高效灵活。
- **结构化日志**：所有内核日志（丢包、调度事件等）输出 JSON 格式，便于远程日志平台（ELK）聚合分析。
- **top 系统监控**：轻量监控工具（Rust 静态编译，<100KB，DESIGN §18.3）作为**必支持**工具，
  数据源为 `/proc/stat`、`/proc/loadavg`、`/proc/<pid>/stat`（DESIGN §10.2）；
  后续提供 **Web 版 `/top` 接口**（经 novos-gateway 暴露）与 **GUI 版系统监视器**（Desktop 版，DESIGN §13.16）。

### 3.3 不可变系统与原子升级

**现状**：OCI 镜像实现应用层 OTA（DESIGN §21.9）。

**长期演进**：

- **A/B 分区升级**：把原子性从应用层扩展到系统层——A/B 根文件系统分区，新系统写非活跃分区，
  升级成功再切换；内核升级中断电也不变砖。
- **系统配置不可变性**：借鉴 Ubuntu Core，整个系统（内核/基础库/部分应用）视为只读镜像，
  用户配置与状态数据存 `/var`/`/data` 可写分区，经 overlayfs/配置覆盖实现定制。

### 3.4 社区驱动需求（硬件连接性 / 服务可靠性 / 远程可运维性）

> 与三条主线不同，本类需求由**真实用户画像**直接驱动（传统嵌入式工程师 / 云原生 DevOps /
> 工业现场维护），集中在三个维度，按紧迫度分三档，全部**只增不减**。详细见 DESIGN §23。

**基础生存型（v1.1–v1.5）**：容器保活策略（`restartPolicy`）、**novos-gateway（HTTP 服务/反向代理）**、
Web UI 默认开启（v1.1）；
4G/5G PPP+USB 串口、持久化日志轮转、WireGuard VPN、存储卷独占锁、MicroPython、PTP/NTP 同步（v1.5）。

**进阶生产力型（v2.0+）**：四层负载均衡（IPVS 轮询）、流量镜像（灰度验证）。

**与三主线的关系**：服务可靠性（保活/持久化日志/独占锁）归入主线一"免疫"；远程可运维性
（Web UI/novos-gateway/VPN）归入主线二"透明"；硬件连接性（4G/5G）与时间同步（PTP/NTP）归入主线三"好用"。
三者共同构成从"原型"到"产品"的完整拼图。

### 3.5 novos-gateway（云原生网关，Rust 实现）

**现状**：轻量 HTTP 服务作为网关控制面（DESIGN §18.3）；Web 管理界面需要稳定的 HTTP/TLS 承载。

**长期演进**：把 HTTP 服务升级为**内置的云原生网关**，降低用户自行搭建反向代理的困惑：

- **能力**：HTTP/1.1（必支持）+ HTTP/2（可选）、TLS（rustls）、反向代理（按路由转发到容器服务）、
  负载均衡（轮询/hash 接口预留，与 DESIGN §23.2 L4LB 分层）；
- **定位**：与"四层负载均衡（L4LB）"形成 **L4（IPVS）+ L7（novos-gateway）** 双层网关；
- **不依赖内核新功能**：仅用现有 TCP 栈 + TLS 套接字，纯用户态静态二进制（DESIGN §18.3）；
- **验收**：`novos gateway` 一条命令启动，把 `/metrics`、`/top`、Web 管理界面统一挂载到 443/80。

---

## 主线四：图形与桌面体验 —— 从"命令行"到"可视交互"

演进哲学：**从"网关专用"到"可穿戴/终端一体机"**；由产品线扩展（DESIGN §2.4 山水观心 Desktop）驱动。

**触发条件（任一满足即启动）**：
- 工业/消费用户提出需要本地可视化操作；
- 用户要求在设备上直接查看监控面板（而非仅 Web）；
- 社区请求提供图形化配置工具。

### 4.1 阶段规划（v2.0+ 起步，内存独立核算）

| 阶段 | 版本 | 内容 | 内存口径 |
|---|---|---|---|
| 阶段一 | v2.0+ | 内核支持帧缓冲 + DRM KMS 最小子集，提供 `/dev/fb0`；用户态最小显示输出（console 切换图形模式） | 内核增量 8–16MB |
| 阶段二 | v3.0 | 轻量 Wayland 合成器 + 窗口管理；提供系统监视器（GUI 版 top） | 用户态图形栈 20–40MB |
| 阶段三 | v4.0 | 完整桌面环境（文件管理器/终端/设置面板），可定制为"信息亭模式"或"桌面模式" | 总目标 **128–256MB** |

> **内存预算**：图形版**独立核算，不再受 32MB 限制**；设计依据 DESIGN §2.4（内存）、§13.16（图形子系统）、
> §19.1（GPU/显示框架架构级预留）、§20.1（GUI 交互通道）。

---

## 演进路线图（四维度总览）

| 维度 | 短期 v1.0 | 中期 v2.0 | 长期 v3.0+ |
|---|---|---|---|
| 致命 Bug | `novos-check` 强制拦截 | 轻量兼容层、自动 FS 转换 | 官方应用商店、Cgroup 策略硬化 |
| 核心困惑 | 文档引导、`/etc/motd` 提示 | 云端构建服务、SMP 负载均衡 | OCI 原生生态、与 Balena 集成 |
| 高频需求 | 调试开关、预留 RT 接口、**top** | SCHED_FIFO、结构化日志、**novos-gateway** | eBPF 子集、A/B 分区原子升级、**GPU/图形支持** |
| 图形与桌面（主线四） | — | 帧缓冲 + DRM KMS、`/dev/fb0` | Wayland 合成器、系统监视器、完整桌面环境 |
